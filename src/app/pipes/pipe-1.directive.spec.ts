import { Pipe1Directive } from './pipe-1.directive';

describe('Pipe1Directive', () => {
  it('should create an instance', () => {
    const directive = new Pipe1Directive();
    expect(directive).toBeTruthy();
  });
});
